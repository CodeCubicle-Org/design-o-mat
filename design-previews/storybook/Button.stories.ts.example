import type { Meta, StoryObj } from '@storybook/your-framework';

// Example mapping from component-preview_config.json entry:
// {
//   "name": "Button",
//   "states": ["default", "hover", "focus", "disabled"],
//   "variants": ["primary", "secondary", "ghost"]
// }
import { Button } from '../src/components/Button';

const meta = {
  title: 'Components/Button',
  component: Button,
  // Component-level defaults (maps to component config defaults)
  args: {
    variant: 'primary',
    state: 'default',
    label: 'Button'
  },
  argTypes: {
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'ghost']
    },
    state: {
      control: 'select',
      options: ['default', 'hover', 'focus', 'disabled']
    }
  }
} satisfies Meta<typeof Button>;

export default meta;
type Story = StoryObj<typeof meta>;

// Story-level overrides (maps to explicit overrides in your JSON)
export const PrimaryDefault: Story = {
  args: {
    variant: 'primary',
    state: 'default',
    label: 'Primary'
  }
};

export const PrimaryHover: Story = {
  args: {
    variant: 'primary',
    state: 'hover',
    label: 'Primary Hover'
  }
};

export const SecondaryDefault: Story = {
  args: {
    variant: 'secondary',
    state: 'default',
    label: 'Secondary'
  }
};

export const GhostDisabled: Story = {
  args: {
    variant: 'ghost',
    state: 'disabled',
    label: 'Ghost Disabled'
  }
};
